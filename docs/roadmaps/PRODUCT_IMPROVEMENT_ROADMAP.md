# Roadmap покращення Pullora

Оновлено: 17 липня 2026 року.

Цей файл є джерелом правди для запланованих покращень Pullora після `v5.2.43`. План не залежить від історії чатів або пам'яті асистента.

## Основні правила

- Українська є мовою за замовчуванням.
- Усі нові тексти повинні мати український та англійський переклади.
- Не змінювати усталений темний дизайн без окремого рішення.
- Спочатку надійність і безпека, потім нові можливості.
- Не додавати залежності, якщо завдання вирішується засобами React, TypeScript, Rust або Tauri.
- Кожен користувацький реліз повинен пройти production-збірку, smoke-test і перевірку обох мов.

## Послідовність релізів

| Версія | Напрям | Стан |
| --- | --- | --- |
| `v5.2.44` | Стабілізація поточного інтерфейсу та безпеки | Випущено |
| `v5.3.0` | Локалізація і зрозумілі помилки | Випущено |
| `v5.4.0` | Безпечне самооновлення | Випущено |
| `v5.5.0` | Надійне встановлення і rollback | Випущено |
| `v5.6.0` | Декомпозиція бібліотеки | Випущено |
| `v5.7.0` | Покращення UX бібліотеки | Випущено |
| `v5.8.0` | Доступність і діагностика | Випущено |
| `v5.9.0` | Масові дії бібліотеки | Випущено |
| `v5.10.0` | Спроба єдиної дизайн-системи | Випущено, відкочено у `v5.10.1` |
| `v5.10.1` | Відновлення дизайну v5.9.0 | Випущено |
| `v5.10.2` | Уніфікація Library без зміни дизайну | Випущено |
| `v5.10.3` | Уніфікація Settings, фонів і щільності | Випущено |
| `v5.11.0` | Уніфікація UI без зміни дизайну | Випущено |
| `v5.12.0` | Безпечна оптимізація без візуального дрейфу | Заплановано |

## Незмінний дизайн-контракт

Канонічний візуальний baseline Pullora — реліз `v5.10.1`, який відновлює перевірений дизайн `v5.9.0`. Усі наступні роботи з UI є внутрішньою уніфікацією, виправленням дефектів або оптимізацією, але не редизайном.

Обов'язково зберігати без візуальних змін:

- прозорі й розмиті підкладки, крізь які помітний загальний фон;
- чинну синю палітру, сині акценти, selection, focus і primary-дії;
- поточну композицію Library, ширину sidebar, hero card, панель основної дії, блоки версій і деталей;
- чинні відступи, висоти, радіуси, типографіку, іконки, щільність і візуальну ієрархію;
- поточний вигляд Settings, About, Install, контекстних і випадаючих меню;
- окремі темну та світлу теми як дві реалізації однієї айдентики, а не два різні продукти;
- незалежність загального фону, hero-фону й обкладинки застосунку.

Заборонено без окремого рішення користувача:

- змінювати палітру, стиль, композицію або пропорції в межах технічного рефакторингу;
- глобально перефарбовувати поверхні кольором обкладинки, hero-фону чи власного фону;
- замінювати наявну розмітку або CSS новою дизайн-системою одним великим кроком;
- вважати скорочення CSS достатньою причиною для зміни зовнішнього вигляду;
- позначати UI-пункт завершеним без порівняння з baseline-знімками.

Допустимі візуальні зміни: лише явно замовлене виправлення конкретного дефекту, доступність, читабельність або адаптація до підтримуваного розміру вікна. Така зміна повинна бути локальною й не змінювати решту екрана.

## v5.2.44 — стабілізація

Мета: випустити поточні виправлення без зміни візуального напрямку Pullora.

- [x] Обмежити висоту карток деталей і коректно обрізати довгий шлях встановлення.
- [x] Увімкнути Content Security Policy для Tauri.
- [x] Обмежити локальний asset protocol папками з обкладинками Pullora.
- [x] Дозволити завантаження лише з GitHub Releases відповідного репозиторію.
- [x] Дозволити самооновлення лише з офіційного репозиторію Pullora.
- [x] Видалити невикористаний shell-плагін і його дозвіл.
- [x] Оновити Vite до безпечної гілки 7.
- [x] Замінити дубльовані `useSettings()` на один `SettingsProvider`.
- [x] Додати Rust-тести перевірки release URL.
- [x] Перевірити зібраний frontend через headless Playwright на розмірах 1000×700, 1280×720 і 1920×1080.
- [x] Перевірити українську й англійську мови та рівність 772 ключів перекладу.
- [x] Підготувати production-артефакти й успішно пройти release-check та smoke-test portable EXE.
- [x] Створити commit, тег і GitHub Release.

Готово, коли:

- картки не розтягуються через довгі значення;
- production-збірка Tauri запускається;
- `npm audit --omit=dev` не має відомих вразливостей;
- усі Rust-тести проходять;
- обидві мови перевірені в застосунку.

## v5.3.0 — локалізація і помилки

Мета: жодних пошкоджених рядків, сирих HTTP-помилок або текстів лише однією мовою.

- [x] Замінити публічні текстові помилки Rust стабільними кодами; внутрішні технічні деталі залишати лише в логах.
- [x] Перекладати коди помилок у React через чинний i18n-шар.
- [x] Перевірити UTF-8 українських рядків у Rust і документації; пошкоджених файлів не виявлено.
- [x] Перевірити кнопки, підказки, діалоги, порожні стани та повідомлення про помилки статичним аудитом і headless smoke-test для `uk` та `en`.
- [x] Додати автоматичний тест рівності ключів `uk` та `en`.
- [x] Прибрати дублікати ключів у словниках.
- [x] Локалізувати NSIS-інсталятор українською та англійською.
- [x] Форматувати дати, числа й розміри відповідно до вибраної мови.

Готово, коли:

- перемикання мови не залишає текстів іншою мовою;
- UI не показує внутрішні Rust, Tauri або HTTP-повідомлення;
- CI відхиляє неповні переклади.

## v5.4.0 — безпечне самооновлення

Мета: Pullora встановлює лише перевірені оновлення й не зберігає секрети відкритим текстом.

- [x] Перенести GitHub token із `config.json` у Windows Credential Manager.
- [x] Передбачити системне сховище секретів для інших платформ.
- [x] Додати SHA-256 для release-артефактів Pullora.
- [x] Перевіряти джерело, SHA-256 і тип файла до запуску.
- [x] Обмежити файлові команди каталогами Pullora та вибраною папкою встановлення.
- [x] Заборонити самооновлення після будь-якої невдалої перевірки.
- [x] Зберігати останній робочий бінарник для відновлення.

Готово, коли змінений файл неможливо встановити як оновлення Pullora.

## v5.5.0 — надійне встановлення

Мета: перерване або невдале встановлення не пошкоджує активну версію застосунку.

- [x] Формалізувати стани: завантаження, перевірка, розпакування, встановлення, перевірка запуску, завершення.
- [x] Автоматично очищати неповні інсталяції.
- [x] Відновлювати попередню версію після помилки.
- [x] Додати повтор невдалого завантаження.
- [x] Обмежити кількість паралельних завантажень.
- [x] Показувати розмір, архітектуру та тип release asset.
- [x] Дозволити пропустити конкретну версію оновлення.
- [x] Показувати release notes до підтвердження оновлення.

Готово, коли примусове переривання кожного етапу залишає реєстр і активну версію у валідному стані.

## v5.6.0 — декомпозиція бібліотеки

Мета: зміна однієї панелі більше не повинна ламати геометрію сусідніх блоків.

Поступово винести з `LibraryPage.tsx`:

- [x] `LibrarySidebar`;
- [x] `LibraryHero`;
- [x] `VersionPanel`;
- [x] `ApplicationDetails`;
- [x] `FolderManager`;
- [x] `BatchUpdatePanel`;
- [x] hooks вибору, фільтрації та оновлень:
  - [x] вибір активного застосунку;
  - [x] фільтрація й сортування;
  - [x] масові оновлення.

Додатково:

- [x] Централізувати повторювані обчислення статусів.
- [x] Додати компонентні тести основних панелей.
- [x] Зафіксувати мінімальні й максимальні розміри карток.
- [x] Не вводити нову глобальну state-бібліотеку без доведеної потреби.

Архітектурне рішення: локальний стан компонентів і тематичні hooks покривають поточні сценарії; нова глобальна state-бібліотека не потрібна.

Готово, коли компоненти можна тестувати окремо, а `LibraryPage` відповідає переважно за компонування сторінки.

## v5.7.0 — UX бібліотеки

Мета: основні дії доступні не більше ніж за два кліки.

- [x] Режими «Усі», «Встановлені», «Оновлення» та «Обране».
- [x] Компактний і звичайний вигляд.
- [x] Сортування за назвою, запуском, встановленням та оновленням.
- [x] Розмір застосунку на диску.
- [x] Контекстне меню: запуск, папка, оновлення, версії, видалення.
- [x] Запам'ятовування вибраного застосунку, фільтрів і позиції прокручування.
- [x] Підтвердження масового оновлення.
- [x] Віртуалізація або `content-visibility` лише якщо вимірювання покаже проблему на великих бібліотеках.
  - Headless-бенчмарк 1000 карток: 129,8 мс без оптимізації та 47,95 мс із `content-visibility`; оптимізацію застосовано лише до карток сайдбара.

## v5.8.0 — доступність і діагностика

Мета: застосунком можна користуватися без миші, а діагностичний звіт не містить секретів.

- [x] Повна навігація клавіатурою.
- [x] Помітний `focus-visible`.
- [x] Focus trap і повернення фокуса після закриття діалогів.
- [x] `aria-live` для прогресу, помилок і завершення операцій.
- [x] Підтримка `prefers-reduced-motion`.
- [x] Автоматична перевірка контрасту тем.
- [x] Екран журналу подій.
- [x] Кнопка «Скопіювати діагностику».
- [x] Автоматичне приховування token і персональних шляхів.
- [x] Ротація старих логів.

## v5.9.0 — масові дії бібліотеки

Мета: виконувати однакову дію для кількох застосунків без повторення операції для кожної картки.

- [x] Додати Ctrl/Shift-вибір, Ctrl+A, Ctrl/Shift+Space та Escape.
- [x] Показувати кількість вибраних і панель дій унизу сайдбара.
- [x] Оновлювати, переміщувати до папки та змінювати стан «Обране» для вибраних застосунків.
- [x] Видаляти вибрані встановлені застосунки після одного підтвердження.
- [x] Очищати всі неактивні версії, не видаляючи активну.
- [x] Показувати локалізований підсумок успішних, пропущених і невдалих операцій.
- [x] Залишати невдалі елементи вибраними для повтору.
- [x] Перевірити клавіатуру, обидві мови, теми, щільності та підтримувані розміри вікна.

Готово, коли всі масові дії використовують чинні безпечні Tauri-команди, панель не перекриває список, а часткова помилка не приховує невдалі елементи.

## v5.10.0 — єдина дизайн-система

Мета: усі теми, вікна, меню та інтерактивні стани Pullora використовують один набір правил замість локальних CSS-виправлень. Візуальний шар дозволено повністю перезбирати, але чинні сервіси, hooks, Tauri-команди, download queue, rollback і SHA-256 перевірки залишаються джерелом поведінки.

> Історичний запис. Реалізація спричинила неприйнятний візуальний дрейф і була повністю відкочена у `v5.10.1`. Позначки `[x]` нижче означають лише те, що робота входила до `v5.10.0`, а не те, що її реалізація збереглася після відкату. Її підхід із одночасною заміною всього візуального шару не можна повторювати; чинним джерелом вигляду є дизайн-контракт вище.

### Архітектура стилів

- [x] Створити канонічну `.pullora-shell` замість паралельних `.sam-shell` і `.cinematic-shell`.
- [x] Видалити мертві `.sam-shell` правила та compatibility-селектори `.cinematic-shell`.
- [x] Розділити CSS на шари `tokens`, `base`, `components`, `features`, `utilities` і `custom`.
- [x] Запровадити семантичні токени для загального фону, трьох рівнів поверхонь, тексту, дій, меж, фокуса, тіней, прозорості, розмиття, статусів, щільності, анімацій і `z-index`.
- [x] Використовувати однакову структуру компонентів у світлій і темній темах; структурний parity захищений автоматичною CSS-перевіркою, а тема змінює лише візуальні значення токенів.
- [x] Повністю перевірити палітру світлої теми: кнопку «Запустити», primary/secondary/destructive/disabled-стани, текст, межі та напівпрозорі підкладки.
- [x] Створити мінімальні спільні примітиви `UiButton`, `UiSurface`, `UiField`, `UiStatus`, `UiMenu`, `UiSelect`, `UiDialog` та `UiEmptyState` без нових runtime-залежностей.

### Єдиний overlay і меню

- [x] Рендерити `UiMenu` та `UiSelect` через portal у `document.body`.
- [x] Підтримати прив'язку overlay до елемента або координат ПКМ, обмеження viewport і автоматичне відкриття в протилежний бік біля країв.
- [x] Закривати overlay через клік назовні, `Escape` або завершення дії та повертати фокус до тригера.
- [x] Повністю замінити поточні контекстні, overflow і popover-меню одним `UiMenu`.
- [x] Уніфікувати відступи, ширину, типографіку, межі, тіні, іконки, роздільники, loading, disabled і destructive-стани.
- [x] Перевести на `UiMenu` ПКМ картки, меню hero card, bulk overflow, меню папок і додаткові дії релізів.
- [x] Підтримати вкладені меню, Context Menu, `Shift+F10`, стрілки, `Home`, `End`, `Enter`, `Space` та `Escape`.

### Випадаючі списки

- [x] Замінити видимі нативні `<select>` на контрольований `UiSelect` із trigger і portal-listbox.
- [x] Підтримати label, help/error text, current value, checkmark, hover, focus, open, selected, disabled і error-стани.
- [x] Обмежувати висоту списку, прокручувати довгі набори й робити список не вужчим за trigger.
- [x] Реалізувати ARIA combobox/listbox/option, `aria-expanded`, `aria-controls`, `aria-selected` та `aria-invalid`.
- [x] Підтримати стрілки, `Home`, `End`, `Enter`, `Space`, `Escape`, `Tab` і typeahead українською та англійською.
- [x] Перевести на `UiSelect` сортування бібліотеки, тему, мову та стратегію вибору release asset.
- [x] Не змішувати action-меню з контролами одиночного вибору й не додавати пошукове поле для коротких списків.

### Незалежні фони

- [x] Розділити загальний фон світлої теми, загальний фон темної теми, hero-фон застосунку й обкладинку застосунку.
- [x] Прибрати передачу hero-фону з Library до кореневого `Layout`.
- [x] Змінювати загальний фон лише через налаштування відповідної теми.
- [x] Застосовувати hero-фон виключно всередині hero card і отримувати його з `fallbackToCover: false`.
- [x] Гарантувати, що заміна обкладинки не змінює hero або загальний фон, а заміна hero-фону не змінює обкладинку чи загальний фон.
- [x] Показувати загальний фон у Library, Settings, About і за діалогами.
- [x] Зберігати помітність фону через прозорі поверхні без втрати контрасту WCAG AA.

Готово, коли в Pullora є один набір UI-примітивів, контекстне меню не обрізається батьківськими панелями, усі короткі списки мають однаковий вигляд, а чотири типи зображень не впливають один на одного.

## v5.10.2 — Library без візуального дрейфу

- [x] Уніфікувати сітку, sidebar, hero, панелі дій, версій і деталей без зміни прозорої синьої айдентики `v5.10.1`.
- [x] Зберегти незалежність загального фону, hero-фону й обкладинки.
- [x] Уніфікувати фільтри, пошук, сортування, normal/compact і масові дії.
- [x] Уніфікувати portal-меню та destructive-діалоги, клавіатуру й повернення фокуса.
- [x] Зафіксувати геометрію тем і щільностей компонентними та headless-тестами на 1000×700, 1280×720 і 1920×1080.

## v5.11.0 — уніфікація без редизайну

Мета: зменшити дублювання й уніфікувати поведінку Library, Install, Settings та About, зберігши pixel-level parity із `v5.10.1`. Рефакторинг виконується по одному компоненту; повна одночасна заміна JSX/CSS заборонена.

### Baseline і порядок міграції

- [x] Зафіксувати еталонні знімки `v5.10.1` для всіх екранів, тем, щільностей, фонів і підтримуваних розмірів.
  - [x] Зафіксувати Library, Settings, About та Install у темній/світлій темах на 1000×700, 1280×720 і 1920×1080 зі стандартним фоном та normal density.
  - [x] Додати Library compact, власний фон у чотирьох робочих областях і відкритий контекстний overlay на всіх підтримуваних розмірах.
  - [x] Додати відкритий overflow-стан меню релізу About у всій матриці.
  - [x] Додати destructive overlay у темній/світлій темах на 1000×700.
  - [x] Зафіксувати trigger/focus-стан нативних dropdown/select у всій матриці та перевірити системний popup поведінковим тестом; popup Chromium/Windows не входить у headless bitmap.
- [x] Для кожного компонента спочатку додати parity-тест поточної поведінки, потім виконувати внутрішню уніфікацію.
- [x] Порівнювати before/after в однаковому середовищі; непередбачена візуальна різниця блокує merge.
- [x] Очікувану локальну різницю дозволяти лише для явно замовленого виправлення та документувати окремо.
- [x] Не мігрувати наступне вікно, доки попереднє не пройшло visual parity, клавіатуру, WCAG і обидві мови.
  - [x] Реєстр доказів і дозволених локальних відмінностей збережено в `docs/visual-baseline/design-contract/README.md`; автоматичний gate `check-v5-11-parity-gates.py` порівнює `v5.10.1` і поточну збірку в 48 однакових станах (4 вікна, 2 теми, 3 розміри, 2 мови).
  - [x] Фінальний прогін: максимальна геометрична різниця 0 px; Library, Settings і Install — без піксельної різниці, About — не більше 0,1% у межах документованих локальних виправлень; 19 headless UI-сценаріїв і component gate пройдено.

### Повторне перенесення виправлень із v5.10.0

Ці пункти були реалізовані всередині невдалого UI-шару `v5.10.0` і могли бути втрачені під час повного відкату до дизайну `v5.9.0`. Їх потрібно перевірити в `v5.10.1` та за потреби перенести окремими локальними змінами без повернення редизайну.

- [x] Скласти пофайловий реєстр виправлень `v5.10.0` і для кожного зафіксувати: збережено у `v5.10.1`, втрачено під час відкату або більше не потрібне.
- [x] Відновити незалежність чотирьох сутностей: загального фону світлої теми, загального фону темної теми, hero-фону та обкладинки застосунку.
- [x] Гарантувати, що зміна обкладинки змінює лише обкладинку, зміна hero-фону — лише hero card, а налаштування фону теми — лише загальний фон відповідної теми.
- [x] Показувати загальний фон у Library, Settings, About, Install і за всіма діалогами; sidebar, основна область та поверхні не повинні повністю перекривати його.
- [x] Використовувати однакову розмітку, сітку, розміри sidebar/hero card і поведінкову логіку в темній та світлій темах; тема змінює лише токени кольору, прозорості, тіні й розмиття.
- [x] Повторно перевірити всю палітру світлої теми, зокрема кнопку «Запустити», текст на ній, disabled/destructive-стани, межі, приглушений текст і WCAG AA.
  - [x] Виправити primary-кнопку Library: синя підкладка й білий текст у normal/hover на всіх підтримуваних розмірах.
  - [x] Перевірити решту primary, disabled, destructive, muted і surface-станів фактичними computed styles у Library, Settings, About та Install.
- [x] Виправити ПКМ та overflow-меню так, щоб вони не обрізалися карткою або sidebar, враховували краї viewport і повертали фокус після закриття, зберігаючи поточний вигляд.
  - [x] Підтвердити portal-рендеринг ПКМ поза sidebar, утримання меню в межах viewport, `Shift+F10`, початковий фокус і повернення фокуса через `Escape`.
  - [x] Перенести overflow-меню релізу About у portal, додати viewport collision, клавіатуру й повернення фокуса без зміни його вигляду.
  - [x] Перенести bulk overflow у portal, уніфікувати overflow-іконку, viewport collision, клавіатуру та повернення фокуса без зміни стилю панелі.
  - [x] Перевести destructive-діалоги в portal, додати безпечний початковий фокус, ARIA-опис, focus trap, захищений `Escape`, повернення фокуса й однакову червону дію в обох темах.
- [x] Уніфікувати поведінку сортування, теми, мови та asset strategy на спільному нативному select-компоненті: зберегти браузерне позиціонування, клавіатуру, focus, disabled/error і чинний дизайн контролів.
- [x] Перевірити, що normal/compact справді відрізняються щільністю, але не змінюють сітку й не спричиняють стрибків sidebar, hero card або панелей.
- [x] Додати окремі регресійні тести для фонів, паритету тем, світлої primary-кнопки, контекстних меню та випадаючих списків до початку рефакторингу відповідного компонента.
  - [x] Додати компонентний контракт незалежності cover/hero та headless-перевірку геометрії тем і світлої primary-кнопки. Окремий parity-тест фіксує однакові `background-position`, `object-position`, розміри hero-фону й обкладинки у світлій/темній темах на `1000×700`, `1280×720`, `1920×1080`.
  - [x] Додати headless-тест portal-позиціонування контекстного меню біля меж viewport.
  - [x] Додати headless-тести overflow-меню About перед і після його виправлення.
  - [x] Додати headless-тест нативних select-контролів: підписи, семантика, клавіатура, focus-visible, варіанти й контрольоване значення Library.
  - [x] Додати headless-тест bulk overflow: portal поза sidebar, межі viewport, disabled-пункти, клавіатура, `Escape` і повернення фокуса.
  - [x] Додати headless і компонентні тести destructive overlay: семантика, busy/error, focus trap, `Escape`, backdrop, focus return і теми.
  - [x] Додати headless-тест світлої палітри: normal/hover primary, disabled, muted-текст, прозорість поверхонь і фактичний WCAG-контраст.

| Виправлення `v5.10.0` | Стан після `v5.10.1` | Рішення для `v5.11.0` |
| --- | --- | --- |
| Окремі глобальні фони світлої й темної тем | Збережено | Не змінювати API та формат локальних даних |
| Hero-фон не підміняє глобальний фон | Було втрачено | Відновлено локально: Library більше не передає hero preview до `Layout` |
| Обкладинка не використовується як fallback hero-фону | Було втрачено | Відновлено через `fallbackToCover: false` та компонентний тест |
| Світла primary-кнопка, текст і контрастні стани | Втрачено разом із новим шаром токенів | Library primary відновлено; решту станів перевіряти окремо без зміни геометрії |
| ПКМ поза межами картки/sidebar | Збережено у чинному `RepoCard` | Portal, viewport collision, `Shift+F10`, початковий фокус і focus return підтверджено headless-матрицею |
| Однакова поведінка коротких випадаючих списків | Втрачено разом із `UiSelect` | Уніфікувати поведінку на чинній розмітці та стилях |
| Автоматична перевірка однакової геометрії тем | Втрачено | Повернуто в headless-матриці; розширити на compact і custom background |
| Повна заміна shell, CSS і всіх UI-примітивів | Відкочено навмисно | Не переносити; суперечить незмінному дизайн-контракту |

### Library

- [x] Зберегти поточну сітку Library, ширину й висоту sidebar та геометрію основної області; виправляти лише підтверджені layout-дефекти.
- [x] Уніфікувати внутрішню реалізацію режимів, пошуку, щільності й сортування без зміни їхнього розташування та вигляду.
- [x] Зберегти поточні normal/compact розміри, відступи, аватари й типографіку як еталон.
- [x] Забезпечити помітні active, selected, bulk-selected і focus-стани; bulk-панель не перекриває список.
- [x] Розділити внутрішні шари hero card, не змінюючи її розмір, градієнт, підкладку, композицію й типографіку.
- [x] Не дозволяти hero-фону виходити за межі картки або замінювати загальний фон.
- [x] Уніфікувати код панелей основної дії, версій і деталей без зміни їхніх поверхонь, геометрії та hooks вибору, фільтрації, сортування й масових дій.

### Встановлення

- [x] Зберегти поточну композицію й оформлення `ReleaseSelector`; змінювати JSX/CSS лише поетапно з visual parity.
- [x] Уніфікувати поведінку діалогу, header/footer і прокручування без зміни їхнього поточного розташування та айдентики.
- [x] Зберегти поточну послідовність «Версія → Файл → Підтвердження → Результат» і можливість повернутися назад; не вводити нову візуальну навігацію кроками.
- [x] Показувати для версії дату, стабільність і статус, а для файла — тип, архітектуру, розмір і сумісність.
- [x] У підтвердженні показувати версію, файл, шлях і попередження; у результаті — success, warning, error, retry та cleanup.
- [x] Залишити одну primary action; «Назад» і «Скасувати» зробити другорядними.
- [x] Не закривати активне завантаження випадковим кліком по overlay.
- [x] На 1000×700 зберігати еталонну композицію `v5.10.1`; локально виправляти лише вихід кнопок за viewport.

### Налаштування

- [x] Зберегти поточну композицію Settings; декомпозувати код секцій без зміни header, навігації, поверхонь і відступів.
- [x] На вузькому вікні зберегти поточну навігацію; змінювати її лише для усунення підтвердженого overflow.
- [x] Зберегти поточне групування «назва — пояснення — control» без зміни геометрії секцій.
- [x] Уніфікувати поведінку theme, language і asset strategy select, зберігши їхній поточний вигляд.
- [x] Прибрати autosave-індикатор і кнопку «Готово»: зберігати зміни у фоні, а помилки показувати в основній області Settings.
- [x] Зберегти чинні налаштування світлої й темної тем, фонів, прозорості, розмиття та щільності без додавання нового preview-блоку.
- [x] Показувати загальний фон у кожній секції й застосовувати щільність до компонентів, меню та списків.
- [x] Зробити фон лаунчера єдиним кореневим фоном для Library, Settings, About, Install і всіх діалогів; локальні фони сторінок та hero-фон не повинні його замінювати.
- [x] Перевести основні підкладки Library, Settings, About та Install на спільні `--surface-1/2/3` і `blur(var(--surface-blur))`; прибрати жорстко задані непрозорі кольори, локальні `rgba(...)` та фіксовані значення blur, щоб повзунки прозорості й розмиття діяли в усіх розділах однаково.
  - [x] Library: основні панелі, toolstrip, hero card, панель дій, інформаційні панелі та внутрішні картки використовують спільну трирівневу surface-ієрархію; власний фон не створює окремих значень прозорості або blur.
  - [x] Settings: workspace, навігація, вміст і секції.
  - [x] About: hero, панелі та список релізів.
  - [x] Install: overlay, діалог, body, footer і картки кроків.
- [x] Додати регресійний headless-тест однакових computed styles і видимості загального фону для Library, Settings, About та Install при прозорості `0/40/80%`, розмитті `0/12/32px`, у світлій і темній темах.
  - [x] Settings: дві теми, три щільності, прозорість `0/40/80%`, розмиття `0/12/32px` і viewport `1000×700`, `1280×720`, `1920×1080`.
  - [x] About: перевірено спільні surface-токени, видимість загального фону, незмінність геометрії, прозорість `0/40/80%` і розмиття `0/12/32px` у світлій та темній темах.
  - [x] Install: перевірено спільні surface-токени, видимість загального фону, незмінність геометрії, прозорість `0/40/80%` і розмиття `0/12/32px` у світлій та темній темах.
  - [x] Library: перевірено sidebar, main pane, toolstrip, hero, панель дій і внутрішні картки; спільний фон видимий, геометрія незмінна, прозорість `0/40/80%` і розмиття `0/12/32px` працюють у світлій та темній темах.
- [x] Розділити сторінку на секційні компоненти, зберігши один settings provider і чинне автозбереження.

### Про застосунок

- [x] Зберегти поточну композицію About; повторно використати спільну поведінку без нового page frame, що змінює вигляд. Зафіксовано структурним і headless parity-контрактом для світлої/темної теми на `1000×700`, `1280×720` і `1920×1080`.
- [x] Зберегти поточний product header, версію, стан оновлення та розташування основних дій. Parity-контракт фіксує назву Pullora, current/latest версії, доступність і порядок фокуса двох дій та однакову геометрію обох тем.
- [x] Уніфікувати фільтри, панель обслуговування, список релізів і статуси версій. Компонентні селектори використовують спільні токени теми без окремої світлої гілки; headless-контракт перевіряє `Усі / Старі / Поточна`, статуси `current / newer / older / missing`, disabled-стан очищення та однакову геометрію обох тем на `1000×700`, `1280×720`, `1920×1080`.
- [x] Уніфікувати поведінку меню й діалогів релізу, не змінюючи їхні розміри, поверхні, відступи та синю акцентну систему. Діалоги release notes і update/rollback рендеряться через portal, використовують спільний focus trap та повертають фокус до вихідної дії; headless-сценарій перевіряє меню, клавіатуру, viewport collision, overlay/Escape, focus return і паритет тем на `1000×700`, `1280×720`, `1920×1080`.
- [x] Зберегти SHA-256, portable asset, rollback і recovery-обмеження без змін. Контракт фіксує точний `SHA256SUMS.txt`, відсікання setup/installer/MSI, блокування update/rollback без portable-файла або manifest, перевірку хеша до підготовки, fail-closed очищення, перевірене резервне копіювання та recovery; Rust-тести й headless-матриця обох тем на `1000×700`, `1280×720`, `1920×1080` пройдені.
- [x] Показувати загальний фон за сторінкою та діалогами. About не створює власного фонового шару, а release notes та update/rollback використовують спільні `--launcher-background-scrim`, `--surface-material-strong` і `--surface-blur`; headless-контракт підтверджує незмінність загального artwork за сторінкою й обома portal-діалогами в темній/світлій темах на `1000×700`, `1280×720`, `1920×1080`.

Готово, коли код чотирьох робочих областей став узгодженішим, але їхній вигляд збігається з `v5.10.1` у всій visual matrix.

## v5.12.0 — безпечна оптимізація без візуального дрейфу

Мета: видалити лише доведено мертвий код і покращити продуктивність, не змінюючи канонічну айдентику, геометрію або прозорість `v5.10.1`.

- [ ] Замінювати дубльовану реалізацію лише після parity-тесту; не міняти контроль тільки заради нового компонента.
- [ ] Видаляти селектори й CSS-гілки лише після підтвердження, що вони не впливають на жоден baseline-сценарій.
- [ ] Зосередити стилі кожної функції в одному feature-шарі; не виправляти один компонент у кількох CSS-файлах.
- [ ] Скорочувати CSS лише там, де це не змінює computed styles; числовий бюджет не має пріоритету над visual parity.
- [ ] Зменшувати дубльовані селектори й `!important` поступово, з перевіркою кожного компонента.
- [ ] Ліниво завантажувати Settings, About, Install і великі detail-діалоги зі збереженням стану Library та scroll position.
- [ ] Утримувати початковий JavaScript у межах 90 КБ gzip.
- [ ] Зберегти `content-visibility`; ціль для layout 1000 карток — до 50 мс.
- [ ] Не додавати глобальних state-бібліотек, `memo` або абстракцій без профілювання й вимірюваної користі.

### Перевірки v5.10–v5.12

- [ ] Додати контрактні тести поведінки й baseline screenshot-тести до будь-якого UI-рефакторингу.
- [ ] Перевірити незалежність загального фону, hero-фону й обкладинки.
- [x] Перевірити чинні контекстні й overflow-меню: краї viewport, ПКМ, клавіатуру, фокус і destructive-стани.
- [ ] Перевірити чинні випадаючі списки: вибір, клавіатуру, прокручування, disabled/error та повернення фокуса.
- [ ] Перевірити Install: stable/preview, unsupported asset, скасування, помилку, retry, cleanup та успіх.
- [ ] Перевірити Settings: autosave, тему, мову, asset strategy, окремі фони, opacity, blur, density та reset.
- [ ] Перевірити About: фільтри, release notes, SHA-256, update і rollback.
- [ ] Виконати headless visual matrix для Library, Settings, About, Install і відкритих overlay у темній/світлій темах, normal/compact, зі стандартним/власним фоном на 1000×700, 1280×720 і 1920×1080 та порівняти її з `v5.10.1`.
- [ ] Окремо перевіряти збереження прозорості, синьої палітри, загального фону, розмірів sidebar/hero і відсутність глобального перефарбування від обкладинки.
- [ ] Перевірити WCAG AA, keyboard-only сценарії, помітний focus і `prefers-reduced-motion`.
- [ ] Перевірити рівність українських та англійських i18n-ключів.

Готово, коли production-збірка проходить без функціональних, accessibility, localization і візуальних регресій відносно `v5.10.1`; скорочення коду саме по собі не є критерієм готовності.

## Обов'язкова перевірка кожного релізу

- [x] `npm run build`
- [x] `cargo fmt --all -- --check`
- [x] `cargo check --locked`
- [x] `cargo test --locked`
- [x] `npm audit --omit=dev`
- [x] `npm run tauri-build`
- [x] `npm run check:release -- -Version <version> -RcReadiness`
- [x] Smoke-test portable EXE
- [x] Українська й англійська мови
- [x] Розміри 1000×700, 1280×720 і 1920×1080
- [x] Темна, світла й автоматична теми та власний фон
- [x] Перевірка встановлення portable EXE, setup/MSI й архіву з EXE
- [x] Перевірка оновлення, rollback і видалення

## Поза поточним планом

- Будь-який редизайн, зміна синьої палітри, прозорості, композиції або поточної айдентики без окремого рішення користувача.
- Повернення AI Workspace.
- Встановлення source code archives як готових застосунків.
- Нова state-management або UI-бібліотека без вимірюваної потреби.
